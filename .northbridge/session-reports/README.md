# Nordi session reports

Add engineering session reports here for NEO learning ingestion.

- Review-only until NEO learning pipeline is live.
- Run `npm run validate-neo-bridge` for a dry-run preview.
- Run `npm run send-neo -- --dry-run` before live ingest.
