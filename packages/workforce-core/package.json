{
  "name": "@northbridge/workforce-core",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable workforce platform core — organization, hierarchy, assignments, permissions (NEO Phase 1)",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@northbridge/workforce-contracts": "file:../workforce-contracts"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
