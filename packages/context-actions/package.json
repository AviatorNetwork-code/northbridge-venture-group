{
  "name": "@northbridge/context-actions",
  "version": "0.1.0",
  "private": true,
  "description": "Context Action Engine — generates context-aware actions from conversation state — vendored for northbridge-venture-group Vercel builds (source: NEOS @northbridge/context-actions)",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@northbridge/conversation-state": "file:../conversation-state"
  }
}
