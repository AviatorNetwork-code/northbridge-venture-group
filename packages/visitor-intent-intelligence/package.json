{
  "name": "@neos/visitor-intent-intelligence",
  "version": "1.0.0",
  "description": "Visitor Intent Intelligence (VII) — infer visitor intent, journey success, and CAT improvement recommendations across Northbridge products.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test tests/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [
    "neos",
    "visitor-intent",
    "customer-intelligence",
    "cat"
  ],
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.4.5"
  }
}
