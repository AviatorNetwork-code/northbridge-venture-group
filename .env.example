# NEO provider configuration
# mock = local mock data (default)
# local = connect to a local NEO service with mock fallback
NEXT_PUBLIC_NEO_PROVIDER=mock
NEXT_PUBLIC_NEO_BASE_URL=http://localhost:4000
